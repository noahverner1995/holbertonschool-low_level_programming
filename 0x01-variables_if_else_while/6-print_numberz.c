#include <stdio.h>

/**
 * main - Print all single digit numbers of base 10 starting from 0 using only the putchar function
 * Return: 0 Success
 */
int main(void)
{
int i;

for (i = 18; i <= 27; i++)
{
putchar(i);
}

putchar('\n');
return (0);
}
